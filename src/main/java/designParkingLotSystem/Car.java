package designParkingLotSystem;

public class Car  extends Vehicle{

    public Car(String vehicleNumber, VehicleType type, VehicleSize size) {
        super(vehicleNumber, type, size);
    }
}
