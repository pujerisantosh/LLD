package designParkingLotSystem;

public class Truck extends  Vehicle{

    public Truck(String vehicleNumber, VehicleType type, VehicleSize size) {
        super(vehicleNumber, type, size);
    }
}
